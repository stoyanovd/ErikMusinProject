<%= form_for(@lettering) do |f| %>
    <% if @lettering.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@lettering.errors.count, "error") %> prohibited this lettering from being saved:</h2>

            <ul>
                <% @lettering.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="field">
        <%= f.label :name %><br>
        <%= f.text_field :name %>
    </div>
    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>
